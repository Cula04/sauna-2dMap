export type Position = { row: number; col: number };
export type NextPositionWithChar = {
  nextRow: number;
  nextCol: number;
  nextChar: string;
};
export type Direction = { row: number; col: number };
